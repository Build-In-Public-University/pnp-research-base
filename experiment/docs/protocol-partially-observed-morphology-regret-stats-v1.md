# Partially observed morphology regret statistics v1

This repeats the declared partially observed fixture over 100 deterministic seeds. Each seed compares the instantaneous heuristic, exact online Bayes policy, and clairvoyant realized-path oracle. The per-seed regret identity is checked before aggregation.
