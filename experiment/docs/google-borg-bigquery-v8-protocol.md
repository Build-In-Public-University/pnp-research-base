# Google Borg BigQuery v8 protocol

Apply the frozen v7 scheduler-stratification query independently to Borg cells `a` and `b`. Use the same first-24-hour window, attempt definition, metadata fields, coarse CPU/memory request bins, and conservation audit. No thresholds or strata are retuned between cells.

This is a paired observational comparison of scheduler composition and outcome profiles. It is not a randomized priority experiment.
